# ─────────────────────────────────────────────────────────
#  Copy this to .env  →  cp .env.example .env
#  All secrets live here. Never commit .env to git.
# ─────────────────────────────────────────────────────────

# PostgreSQL — your VPS server
# asyncpg driver for FastAPI (async)
DATABASE_URL=postgresql+asyncpg://admin:StrongPass@123@127.0.0.1:5433/appdb

# psycopg2 driver for Alembic migrations (sync only)
DATABASE_SYNC_URL=postgresql://admin:StrongPass@123@127.0.0.1:5433/appdb

# JWT — generate a strong secret:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace_me_with_output_of_above_command
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

#CORS — your React frontend URL
# In production: your actual domain e.g. https://vikasana.org
# In dev: http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# App
APP_ENV=production
DEBUG=false
